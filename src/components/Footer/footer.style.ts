import {StyleSheet} from "react-native";

export default StyleSheet.create({
    footerContainer: {
        position: "absolute",
        bottom: 16,
        left: 16,
        right: 16,
        // paddingHorizontal: 16,
    },
    footerText: {
        fontFamily: "monospace",
        color: "#FFFFFFBB", // a bit more opacity for better readability
    },
});
